import type { FC } from 'react';

export const PaginaInfo: FC = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '50vh',
        fontSize: '3rem',
        fontWeight: 'bold',
        color: '#007a4d',
      }}
    >
      hiiii 
    </div>
  );
};

export default PaginaInfo;