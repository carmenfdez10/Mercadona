import Body from './componentes/Body';

const Web = () => {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', minHeight: '100vh' }}>
      <Body />
    </div>
  );
};

export default Web;
